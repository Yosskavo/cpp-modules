#include "PhoneBook.hpp"
#include <iostream>

std::string	get_string()
{
	std::string	tmp;

	while (1)
	{
		if (std::getline(std::cin, tmp))
		{
			
		}
	}
}

int add_contact(PhoneBook phone)
{
	std::string	tmp;

	return (0);
}
